<h1>Products</h1>
 
<table>
	<tr>
		<th>Title</th>
		<th>Price</th>
		<th></th>
		<th></th>
	</tr>

	<% @products.each do |product| %>
	<tr>
		<td><%= product.id %></td>
		<td><%= product.title %></td>
		<td><%= number_to_currency(product.price.to_f, unit: "£", precision: 2) %></td>
		<td><%= link_to 'Show', product %></td>
		<td><%= link_to 'Edit', edit_product_path(product) %></td>
	</tr>
	<% end %>
</table>
